export default class ActivityTypes {
    private baseUrl;
    private authToken;
    constructor(authToken: string);
    add(params: {
        name: string;
        iconKey: string;
        color: string;
    }): Promise<any>;
    all(params: {}): Promise<any>;
}
