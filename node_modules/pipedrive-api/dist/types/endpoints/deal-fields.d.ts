export default class DealFields {
    private baseUrl;
    private authToken;
    constructor(authToken: string);
    all(params: {
        start?: number;
        limit?: number;
    }): Promise<any>;
}
