export default class Pipelines {
    private baseUrl;
    private authToken;
    constructor(authToken: string);
    all(params: {
        sinceTimestamp: Date;
        items?: string;
        start?: number;
        limit?: number;
    }): Promise<any>;
}
