export default class Leads {
    private baseUrl;
    private authToken;
    constructor(authToken: string);
    all(params?: {
        start?: number;
        limit?: number;
        archivedStatus?: string;
    }): Promise<any>;
}
