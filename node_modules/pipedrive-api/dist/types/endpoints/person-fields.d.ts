export default class PersonFields {
    private baseUrl;
    private authToken;
    constructor(authToken: string);
    all(): Promise<any>;
    add(params: {
        name: string;
        options: any;
        fieldType: string;
    }): Promise<any>;
}
