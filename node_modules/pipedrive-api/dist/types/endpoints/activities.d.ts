export default class Activities {
    private baseUrl;
    private authToken;
    constructor(authToken: string);
    add(params: {
        subject: string;
        note?: string;
        type: string;
        done?: number;
        dueDate?: string;
        dueTime?: string;
        duration?: string;
        userId?: string;
        dealId?: string;
        orgId?: number;
        personId?: string;
        location?: string;
        participants?: [];
        busyFlag?: boolean;
        attendees?: [];
        publicDescription?: string;
    }): Promise<any>;
}
