/// <reference types="node" />
import { EventEmitter } from 'events';
import { OAuth2 } from './oauth';
export default class Connection {
    events: EventEmitter;
    oauth2: OAuth2;
    endpoints: any;
    constructor(auth: {
        clientId: string;
        clientSecret: string;
        lastKnownAuthState?: {
            accessToken: string;
            refreshToken: string;
            expirationTime: Date;
        };
    }, options?: {
        setRefreshManually?: boolean;
    });
    endpoint(type: string): Promise<any>;
}
